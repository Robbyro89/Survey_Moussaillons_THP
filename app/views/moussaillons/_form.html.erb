<% if @mousaillon.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@mousaillon.errors.count, "error") %> prohibited
        this content from being saved:
      </h2>
      <ul>
        <% @mousaillon.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
